{
  "name": "bap-post-deploy-verify-playwright",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright runner for the Mode C of bap-post-deploy-verify. Generated specs per finding live under playwright-tests/.",
  "scripts": {
    "verify": "playwright test --reporter=json",
    "verify:headed": "playwright test --headed",
    "auth:bootstrap": "playwright codegen $HEYBAP_URL --save-storage=$HOME/.heybap-playwright-auth.json",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
